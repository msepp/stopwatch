import { TaskDurationPipe } from './task-duration.pipe';

describe('TaskDurationPipe', () => {
  it('create an instance', () => {
    const pipe = new TaskDurationPipe();
    expect(pipe).toBeTruthy();
  });
});
